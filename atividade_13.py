# ### Atividade 13 — Analisando condições

# Considere:

# ```python
# idade = 18
# possui_cracha = True
# esta_bloqueado = False
# ```

# Antes de executar o programa, determine se cada expressão resultará em `True` ou `False`.

# ```python
# idade >= 18 and possui_cracha
# idade < 18 or possui_cracha
# not esta_bloqueado
# idade >= 18 and not esta_bloqueado
# possui_cracha and esta_bloqueado
# ```

# Depois, execute o código e compare os resultados com suas previsões.

idade = 18
possui_cracha = True
esta_bloqueado = False

print(idade >= 18 and possui_cracha)
print(idade < 18 or possui_cracha)
print(not esta_bloqueado)
print(idade >= 18 and not esta_bloqueado)
print(possui_cracha and esta_bloqueado)